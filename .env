# ── Database credentials ─────────────────────────────────────
DB_ROOT_PASS=rootStrongPass123!
DB_USER=cafeuser
DB_PASS=cafePass456!

# ── Gmail (for user approval emails + password reset) ─────────
MAIL_USER=      # ⬅️ your Gmail address
APP_PASS=        # ⬅️ Gmail App Password (see below)

# ── EC2 Public IP — replace with YOUR actual EC2 Public IP ───
# Find it in EC2 Console → Instances → Public IPv4 address
ALLOWED_ORIGIN=http://35.175.193.57    # ⬅️ your EC2 IP
FRONTEND_URL=http://35.175.193.57     # ⬅️ your EC2 IP
